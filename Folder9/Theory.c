/*Strings
A character array terminated by a '\0' (null character)
\0 in the last of an aray means all the letters of an array are together

for eg:
char  name[] = {'r','u','d','r','a','\0'}
means:  rudra

IN c, single characters are written in single quotes , and strings
are written in double quotes

IN memory, all are stores in memory blocl, and null character is 
also stored

Format Specifier----> %s
char name[] ] "Rudra";
printf("%s",name);
scanf("%s",name);

❌ " " is a string
✅ ' ' is a character */