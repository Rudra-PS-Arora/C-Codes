/*gets(str) ----> Takes a str( even multiword) ---> puts() --->prints the string
but gets() is dangerous and outdated 

fgets(str, n,file) 
in place of file write: stdin (standard input)
n: means the max no of characters

stop when n-1 chars input or new line entered
*/
