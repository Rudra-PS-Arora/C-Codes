/*Syntax:
struct student ECE[100];
struct student is the data type and ECE[100] is the array

Acess:
ECE[0].roll=200;*/

//Array of structures = multiple copies of the same structure, 
//each copy storing its own values for (name, roll_no, cgpa).
